services:

  postgres:

    image: postgres:17

    container_name: carnotes-db

    restart: always

    environment:
      POSTGRES_DB: carnotes
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres

    ports:
      - "5432:5432"

    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: